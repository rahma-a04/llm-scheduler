# llm-scheduler
CS 4701 final project

# Install dependecies
- cd into frontend
- Run `python install.py` to install dependencies
- Run `python uninstall.py` to uninstall dependencies
- Run`python cleanup.py` to clean up the directory

# Frontend 
Running the app:
- cd into frontend
- Run `streamlit run app.py`